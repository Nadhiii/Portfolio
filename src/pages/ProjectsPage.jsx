import React from 'react';
import Projects from '../components/Projects'; // We are reusing the component we already built

const ProjectsPage = () => {
  return <Projects />;
};

export default ProjectsPage;